apiVersion: v1
kind: Pod
metadata:
  name: pod-test
  labels:
    app: pod-test
spec:
  containers:
  - name: myapp
    image: busybox
    command: ['sh', '-c', 'echo Hello Kubernetes! && sleep 3600']
